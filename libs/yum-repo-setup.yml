---
# ./libs/yum-repo-setup.yml

- name: Yum repo setup
  hosts: all
  gather_facts: false # remove later! speeds up testing
  become: true

  tasks:
    - name: Enable YUM Repos
      template:
        src: open_three-tier-app.repo
        dest: /etc/yum.repos.d/open_three-tier-app.repo
        mode: 0644