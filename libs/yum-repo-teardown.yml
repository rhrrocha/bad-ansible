---
# ./libs/yum-repo-teardown.yml

- name: Reverse Yum repo setup
  hosts: all
  gather_facts: false # remove later! speeds up testing
  become: true

  tasks:
    - name: Reverse Enable YUM Repos
      file:
        path: /etc/yum.repos.d/open_three-tier-app.repo
        state: absent