---
# ./libs/deploy-http-server.yml

- name: Deploy Http Server
  hosts: apps
  gather_facts: false
  become: true
  vars_files:
    - ../vars/variables.yml

  tasks:
    - name: Install Http Server
      yum:
        name: "{{ http_server_service }}"
        state: latest

    - name: Enable Http Server at boot
      service:
        name: "{{ http_server_service }}"
        enabled: yes

    - name: Start Http Server
      service:
        name: "{{ http_server_service }}"
        state: started